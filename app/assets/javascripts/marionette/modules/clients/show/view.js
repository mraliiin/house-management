HousePad.TradeApp.module("Clients.ShowClient", function(ShowClient, TradeApp, Backbone, Marionette, $, _){

    ShowClient.ShowClientView = Marionette.ItemView.extend({
        template: "#show-client-template",
        tagName: "div",
        className: "row",

        ui: {
        },

        triggers: {
        },

        events: {
        }

    });
});